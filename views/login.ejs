<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Easytel Login</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <header>
        <img src="/images/logo.png" alt="logo">
    </header>
    <% if (locals.message) { %>
        <span class="error message"><%= locals.message %> </span>
    <% } %>
    <form action="/login" id="login-form" method="POST" autocomplete="" class="section-box">

        <div class="input-group">
            <label for="username">User Name:</label>
            <input type="text" id="username" name="username" value="<%= locals.username %>" class="textbox" required>
        </div>

        <div class="input-group">
            <label for="password">Password:</label>
            <input type="password" id="password" name="password" value="<%= locals.password %>" class="textbox" required>
        </div>

    </form>
    <input type="submit" form="login-form" name="login" value="Login">
    
    <br><p>No account yet? <a href="/register">Register</a></p>

</body>
</html>